---
interface Props {
	as?: string;
	class?: string;
}

const {
	as = "p",
	class: className
} = Astro.props;
const Tag = as;
---

<Tag class:list={["type-display-callout", className]}>
	<slot />
</Tag>
