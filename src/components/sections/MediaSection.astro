---
interface Props {
	src: string;
	alt: string;
	class?: string;
	imageClass?: string;
	loading?: "eager" | "lazy";
}

const {
	src,
	alt,
	class: className,
	imageClass,
	loading = "lazy"
} = Astro.props;
---

<section
	class:list={[
		"mt-14 w-full overflow-hidden rounded-xl bg-surface-raised/80 shadow-hairline",
		className
	]}
>
	<img
		src={src}
		alt={alt}
		class:list={["h-auto w-full", imageClass]}
		loading={loading}
	/>
</section>
