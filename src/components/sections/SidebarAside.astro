---
interface Props {
	as?: string;
	class?: string;
}

const {
	as = "aside",
	class: className
} = Astro.props;

const Tag = as;
---

<Tag class:list={["mt-8 border-l border-stone-200 py-2 pl-6 lg:mt-0 lg:h-full lg:pl-8", className]}>
	<slot />
</Tag>
